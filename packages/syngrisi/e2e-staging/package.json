{
  "name": "@syngrisi/e2e-staging",
  "version": "1.0.0",
  "private": true,
  "description": "Staging E2E tests for Syngrisi",
  "scripts": {
    "test": "npx bddgen && npx playwright test",
    "test:smoke": "npx bddgen && npx playwright test --project staging-smoke",
    "test:extended": "npx bddgen && npx playwright test --project staging-extended",
    "test:demo": "npx bddgen && npx playwright test --project staging-demo",
    "test:demo:debug": "SKIP_DEMO_TESTS=true npx bddgen && npx playwright test --project staging-demo",
    "bddgen": "npx bddgen"
  },
  "dependencies": {
    "@playwright/test": "^1.49.0",
    "playwright-bdd": "^8.2.1",
    "dotenv": "^17.2.3",
    "got-cjs": "^12.5.4",
    "form-data": "^4.0.0"
  }
}
