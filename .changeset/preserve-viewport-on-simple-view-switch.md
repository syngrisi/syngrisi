---
"@syngrisi/syngrisi": patch
---

feat(ui): preserve zoom and pan when switching between simple views (actual / expected / diff). Also simplifies `loadRegionsData` by removing dead snapshot code that re-serialized the same state twice.
