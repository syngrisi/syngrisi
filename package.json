{
  "name": "syngrisi-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --scope=syngrisi",
    "start": "lerna run start --scope=syngrisi",
    "build": "lerna run build",
    "graph": "nx graph"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.1"
  }
}
